# This is a Procfile used for deploying applications to platforms like Heroku.
# It specifies the process types and the commands to run them.
# In this case, it defines a worker process that runs the `bot.py` script using Python.
worker: python bot.py